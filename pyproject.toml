[project]
name = "playground"
version = "0.1.0"
description = "Personal MCP servers deployed to AWS Lambda"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[tool.uv.workspace]
members = ["packages/*"]
